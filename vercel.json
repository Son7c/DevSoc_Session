{
  "buildCommand": "npm run vercel-build && npm install --prefix server",
  "outputDirectory": "client/dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/v1/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
